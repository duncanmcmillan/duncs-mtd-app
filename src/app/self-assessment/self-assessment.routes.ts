import { Routes } from '@angular/router';
import { SelfAssessmentComponent } from './component/self-assessment.component';

export const SELF_ASSESSMENT_ROUTES: Routes = [
  { path: '', component: SelfAssessmentComponent },
];
