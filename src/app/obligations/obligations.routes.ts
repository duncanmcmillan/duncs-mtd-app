import { Routes } from '@angular/router';
import { ObligationsComponent } from './component/obligations.component';

export const OBLIGATIONS_ROUTES: Routes = [
  { path: '', component: ObligationsComponent },
];
