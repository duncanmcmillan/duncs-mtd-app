/**
 * @fileoverview Stub service for the Manual data-entry method.
 * No API calls — data is entered directly by the user in the UI.
 * Ready for Claude API integration in a follow-up PR.
 */
import { Injectable } from '@angular/core';

/**
 * Service stub for manual data entry.
 * Placeholder — no implementation required until manual entry UI is wired up.
 */
@Injectable({ providedIn: 'root' })
export class ManualService {}
