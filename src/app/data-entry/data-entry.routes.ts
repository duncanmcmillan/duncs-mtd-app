import { Routes } from '@angular/router';
import { DataEntryComponent } from './component/data-entry.component';

export const DATA_ENTRY_ROUTES: Routes = [
  { path: '', component: DataEntryComponent },
];
